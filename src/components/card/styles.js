import { StyleSheet } from "react-native";
import { width } from "../../utils/common";

export default StyleSheet.create({
  box: {
    height: 200,
    position: "absolute",
    width: width - 10,
  },
});
