import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
    height: 400,
    justifyContent: "center",
    width: "100%",
  },
  parent: {},
});
